#include <tunables/global>

profile mower_mqtt_bridge flags=(attach_disconnected,mediate_deleted) {

  #include <abstractions/base>

  # Capabilities
  file,
  signal (receive) set=(kill,term,int,hup,cont),

  # S6-Overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Bashio
  /usr/lib/bashio/** ix,
  /tmp/** rwk,

  # Access to files
  /data/** rw,

  # Access required for service functionality
  /bin/bash rix,
  /bin/echo ix,
  /etc/passwd r,
}